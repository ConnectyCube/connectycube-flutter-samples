name: p2p_call_sample
description: P2P Calls Sample

version: 0.1.0

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  connectycube_sdk: ^0.2.0-beta3
  flutter_apns:
    git:
      url:
        git://github.com/mwaylabs/flutter-apns.git
  flutter_voip_push_notification: ^0.0.3
  flutter_call_kit: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true